# Passio Nutrition-AI SDK is on GitHub's private npm registry.
# Set GITHUB_TOKEN to a PAT with read:packages scope before running npm install.
@passiolife:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
